$(document).ready(function() {	$('#main-menu').smartmenus();	//tab list	$( '.tab-list').each(function() {		var $this = $(this);		var $tab = $this.find('button.active');		var $link= $tab.find('a');		var $panel = $($link.attr('href'));			$this.on('click' , '.tab-control', function(e) {			e.preventDefault();			var $link = $(this);			var id = this.hash;			if (id && !$link.is('.active')) {				$panel.removeClass('active');				$tab.removeClass('active');				$panel = $(id).addClass('active'); 				$tab = $link.parent().addClass('active');			}						if(($link.attr('href')) == "#tab-2")				$(".current-focus").focus();				});	});		// Wait for the DOM to be ready	$(function() {	  // Initialize form validation on the registration form.	  // It has the name attribute "registration"	  $("#signupForm").validate({		// Specify validation rules		rules: {		  // The key name on the left side is the name attribute		  // of an input field. Validation rules are defined		  // on the right side		  name: "required",		  email: {			required: true,			// Specify that email should be validated			// by the built-in "email" rule			email: true		  },			agree: "required",			topic: {required: "#newsletter:checked"},		},		// Specify validation error messages		messages: {		  name: "Please enter your name",		  email: "Please enter a valid email address"		},		// Make sure the form is submitted to the destination defined		// in the "action" attribute of the form when valid		submitHandler: function(form) {			alert("Thank you for your response!"); 		  form.submit();		}	  });	});				//code to hide topic selection, disable for demo		var isQuestion = $("#is-question");		// newsletter topics are optional, hide at first		var inital = isQuestion.is(":checked");		var topics = $("#newsletter_topics")[inital ? "removeClass" : "addClass"]("gray");		var topicInputs = topics.find("input").attr("disabled", !inital);		// show when newsletter is checked		isQuestion.click(function() {			topics[this.checked ? "removeClass" : "addClass"]("gray");			topicInputs.attr("disabled", !this.checked);		});				var inital = $("#have-website").is(":checked");		var url = $("#have-website-fields")[inital ? "removeClass" : "addClass"]("gray");		$("#have-website").click(function() {			url[this.checked ? "removeClass" : "addClass"]("gray");		});				$(".input").click(function() {			$(".current-focus").removeClass("current-focus")			$(this).addClass("current-focus")		});		});